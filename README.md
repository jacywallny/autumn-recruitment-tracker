# 秋招进度追踪

一个无需安装依赖的本地秋招投递管理网页。

## 使用

直接双击 `index.html`，或在当前目录启动静态服务器：

```powershell
python -m http.server 4173
```

浏览器访问 <http://127.0.0.1:4173>。

数据保存在浏览器 `localStorage` 中。页面支持：

- 新增、编辑、删除投递记录
- 按公司或岗位搜索
- 按状态和求职类型筛选
- 按公司、岗位和日期排序
- 自动计算投递、面试与 Offer 数据
- 将当前筛选结果导出为 UTF-8 CSV
